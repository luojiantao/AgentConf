# 技术名/stack-id

一句话：这套栈用来实现什么，不写职业身份。

## 选用

- 什么时候用这套栈
- 和哪些栈一起出现、谁主谁辅

## 约定

只写实现时必须遵守的约定和易错点，不写教科书。

- 目录 / 命名 / 入口
- 错误处理、日志、测试的默认做法
- 明确禁止的写法

## 与项目对齐

本文件是全局默认。仓库若有 `.pi/Stack/` 下的同名文件，以项目文件为准。
仍以当前仓库已有代码为准；项目私有名词和领域规则属于 `{项目}/.pi/knowledge/`，不要写进本文件。
